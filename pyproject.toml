[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "calibration_pipeline_ao"
version = "0.7.0"

[tool.setuptools]
packages = ["_configs", "_utils"]